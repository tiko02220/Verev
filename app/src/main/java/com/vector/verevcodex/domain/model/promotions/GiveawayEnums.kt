package com.vector.verevcodex.domain.model.promotions

enum class GiveawayType {
    BONUS_POINTS,
    DISCOUNT_PERCENT,
    REWARD,
    COUPON,
}

enum class GiveawaySendMode {
    IMMEDIATE,
    SCHEDULED,
}
